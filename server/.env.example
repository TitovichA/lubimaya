# Скопируйте в .env на сервере (рядом с public_html или в public_html).
# Файл .env не должен попадать в git и в клиентский бандл.

APP_LOGIN=admin
APP_PASSWORD=change-me-now
AUTH_MAX_ATTEMPTS=5
AUTH_LOCKOUT_SECONDS=900
